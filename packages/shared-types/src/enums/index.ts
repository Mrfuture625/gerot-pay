export enum CardType {
  VIRTUAL = "VIRTUAL",
  PHYSICAL = "PHYSICAL",
}

export enum PaymentToken {
  ETH = "ETH",
  USDC = "USDC",
  USDT = "USDT",
}

export enum RewardType {
  SIGNUP = "SIGNUP",
  PURCHASE = "PURCHASE",
  REFERRAL = "REFERRAL",
}